{% extends 'base.html.twig' %}
{% block body %}
<h3> INSCRIPTION SUR LE SITE</h3>
{{form_start(form)}}

    {# il suffit de créer le form avec form_widget
     {{form_widget(form)}}
    <button type="submit" class="btn btn-success">Inscription</button>
     #}
     {# Mais si on veut personnaliser notre case input, on utilise form_row #}

    {{form_row(form.username, {'label': 'Nom d\'utilisateur', 'attr': {'placeholder': 'Votre nom!'}}) }}
    {{form_row(form.email, {'label': 'Votre email', 'attr': {'placeholder': 'Votre adresse email!'}}) }}
    {{form_row(form.password, {'label': 'Votre mot de passe','attr': {'placeholder': 'Votre password!'}}) }}
    {{form_row(form.confirm_password, {'label': 'Confimer votre mot de passe', 'attr': {'placeholder': 'confirmer votre password!'}}) }}

<button type="submit" class="btn btn-success">Inscription</button>
{{form_end(form)}}
{% endblock body %}